const initialState = {
    auth: {
        showLoading: false,
        isUserLoggedIn: false,
    },
    error: {
        code: 0,
        message: "",
        showModal: false
    }
}

export default initialState;